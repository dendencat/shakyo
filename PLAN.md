# 写経支援ツール — MVP実装計画

## Context

本リポジトリ `shakyo` は、**プログラミングコードの写経(タイピングによる書き写し学習)を支援するツール**を開発するプロジェクトである。本計画は unknowns-discovery スキルのインタビューでユーザーと確定した要件に基づく(2026-07-08)。

### 確定した要件

| 項目 | 決定内容 |
|---|---|
| 写経の対象 | プログラミングコード |
| 形態 | **Webアプリとして開発し、後日Tauri化**(WSL2でのTauri開発の摩擦を回避し、検証ループを最速化)。※Tauri v2によるデスクトップ配布は実施済み(2026-07-15、詳細は「MVP後の改善」参照) |
| お手本の参照 | ① ローカルファイル(テキスト/コード/PDF)を開く ② アプリ内でWebページを参照 |
| Web参照方式 | ブラウザ版はiframe埋め込み+別ウィンドウ案内。Tauri版はメインウィンドウのお手本ペイン内の子Webviewで表示(2026-09-08実装、Linux/WSLgで基本操作確認済み。Windows/macOSは実機未確認) |
| MVPのフィードバック機能 | **コードの意味解説のみ**(リアルタイム正誤判定・統計・進捗永続化はMVP対象外) |
| 解説の実現 | **OpenAI API連携(デフォルトモデル: gpt-5.4-mini、変更可能)**。APIキーはユーザー自身が用意する。v2.3.2からブラウザ版はメモリ、デスクトップ版はOSの資格情報ストアで保管する |

### アプリの姿(MVP)

画面を左右に分割し、**左ペイン=お手本ビューア**(ファイル or Webページ)、**右ペイン=写経エディタ**(シンタックスハイライト付き)。写経したコードを選択して「解説」ボタンを押すと、OpenAI APIがコードの意味を日本語で解説するパネルが開く。

## 技術スタック

- **Vite + React + TypeScript** — 純粋な静的フロントエンド(サーバー不要)。Tauri化はこの構成をそのまま包むだけで済む
- **CodeMirror 6** — 写経エディタ。軽量で多言語シンタックスハイライト対応
- **pdf.js** — PDFお手本の表示
- **OpenAI API** — ブラウザ版はフロントエンドから直接、デスクトップ版は信頼するメイン画面からTauriコマンド経由で呼び出す。キーの保存方式と旧データ移行は [docs/api-privacy.md](docs/api-privacy.md) を参照

## 実装ステップ

### 1. プロジェクト基盤ドキュメントの作成(最優先)
- `PLAN.md`(本ファイル): 実装計画の正典
- `AGENTS.md`: AIエージェント向けプロジェクトガイド(目的、スタック、開発コマンド、サブエージェント運用ワークフロー)
- `CLAUDE.md`: `ln -s AGENTS.md CLAUDE.md` でシンボリックリンク(単一ソースで両規格に対応)

### 2. README.md の書き直し
写経支援ツールの説明として全面書き換え: 目的、機能(MVP)、起動方法、APIキー設定手順、ロードマップ。サブエージェント関連の記述はAGENTS.mdへ移設。

### 3. プロジェクト scaffolding
Vite + React + TS の雛形を作成(`npm create vite`)。ESLint/最小構成。

### 4. 分割レイアウトとお手本ビューア(左ペイン)
- 左右分割(ドラッグでリサイズ可能)
- ファイルタブ: テキスト/コードファイルを読み込み表示(ハイライト付き)。PDFはpdf.jsで描画
- Webタブ: URL入力→iframe表示。読み込み失敗(X-Frame-Options等)を検知したら「別ウィンドウで開いて画面分割してください」の案内+ワンクリックで別ウィンドウ起動

### 5. 写経エディタ(右ペイン)
CodeMirror 6。言語選択(または拡張子から自動判定)でハイライト。入力内容はlocalStorageに自動保存(誤リロード対策の下書き保持)。

### 6. コード解説機能
- 設定ダイアログ: OpenAI APIキー、モデル名(デフォルト `gpt-5.4-mini`)をlocalStorageに保存
- エディタで選択した範囲(未選択なら全文)を「このコードの意味を初学者向けに日本語で解説」プロンプトでOpenAI APIへ送信
- 解説パネルにストリーミング表示。エラー(キー未設定・401等)は日本語で案内

### 7. 検証
- `npm run dev` で起動し、実際のフローを通す: サンプルコードファイルを開く→写経する→解説を取得する
- iframe拒否サイト(github.com等)でフォールバック案内が出ることを確認

## MVP対象外(将来拡張)

- 内蔵Webviewの追加実機確認(Windows/macOSで表示・操作、Linuxを含む全レイアウト・DPI変更。Linux/WSLgの基本操作確認は下記参照)
- リアルタイム正誤判定・WPM/正確率統計・進捗の永続化
- 組み込みサンプルコード集

## MVP後の改善

- v2.0.0: Extension API v1をメジャーアップデートとして提供。筆で`{}`を書く新アイコンをデスクトップ配布とWeb faviconに適用。

- Extension API v1: 手動ZIP導入、実行前の構造/AST検査とハッシュ紐付け、明示的な権限同意、Worker内QuickJS WASM実行、セッションに結び付いたホストAPIを追加。コマンド・エディタ操作・お手本追加・ホストUI・専用設定/保存領域・同梱アセット・許可HTTPS通信に対応。管理画面・サンプル・SDK仕様は [docs/extensions.md](docs/extensions.md)。署名・マーケットプレイス・任意パネル・ネイティブAPIは対象外。

- Web参照タブの内蔵Webview化: Tauriの`Window::add_child`でメインウィンドウのペイン内に配置。レイアウト・スクロール・拡大率に追従し、ダイアログ/ドラッグ中は非表示、タブを離れると終了。参照ページにはアプリ権限を付与せず、HTTP(S)以外やローカルアプリへの遷移・ポップアップを拒否。ブラウザ版はiframeを継続。Tauri 2.11.5のunstable API使用につきOS別実機確認を残す(2026-09-08)
- 内蔵WebviewのLinux/WSLg確認: `X-Frame-Options: DENY`と`frame-ancestors 'none'`を返す固定テストページをペイン内に表示。参照ページ・エディタへの入力、設定開閉時の非表示/復帰とページ内入力の保持、1280×800から1100×700への縮小、Webタブ終了・再表示を確認。公開サイト固有の挙動、Windows/macOS、全レイアウト、DPI変更は未確認(2026-09-08)
- 3ペインの配置変更: ヘッダーの「レイアウト」から横3列・縦3段・左右／上下の組み合わせ計6パターンと各枠の内容を選択。各ペインの操作状態を保持し、境界のドラッグ／キーボードによるサイズ調整、配置とパターン別サイズのブラウザ保存、初期配置への復帰に対応(2026-09-07)
- 解説パネルをMarkdownリッチ表示に対応(react-markdown + remark-gfm、2026-07-08)
- エディタ/解説パネル境界の縦リサイズ対応(SplitPane汎用化、2026-07-09)
- 写経内容の名前を付けて保存(アプリ内スナップショット+ファイルダウンロード、2026-07-09)
- 解説のコピーボタン(2026-07-09)
- 貼り付けタブ: 左ペインにテキストを直接貼り付けてお手本にできる(確定後は読み取り専用表示、localStorage復元付き、2026-07-12)
- 解説への追加質問: 解説パネル下部の入力欄から会話文脈付きで追質問できる(ストリーミング回答、コピーはQ&A込み、2026-07-12)
- Webタブの検索履歴+ブックマーク: URL欄下のインライン開閉パネルで履歴(自動記録・最大20件・個別/全削除)とブックマーク(追加/編集/削除)を管理(2026-07-12)
- **Tauri v2によるデスクトップアプリ化**: `src-tauri/` スキャフォールドと `npm run tauri` スクリプトを追加。`app-v*` タグのpushでGitHub Actions(`.github/workflows/desktop.yml`)がWindows/macOS/Linux向けインストーラをビルドしdraft releaseに添付(コード署名なし)。別ウィンドウで開く操作は `@tauri-apps/plugin-opener` でOS既定ブラウザを開く。2026-07-15の時点ではiframe方式を継続し、ペイン内Webviewは2026-09-08に追加。


## v2.1.0 実装・受け入れ条件

- Appと既存メニュー: 常設の名称・説明ヘッダーを削除し、アイコン列と開閉式サイドバーへ統合する。同じ機能で閉じ、別の機能で切り替える。ファイル・保存・Extension・レイアウト・テーマ・設定・ヘルプを収める。
- アイコン: 統一したSVGのサイズ・線幅・余白を使う。設定は歯車、ヘルプは?、Extensionは四角形の組合せ、ライトは太陽、ダークは月。ツールチップ、読み上げラベル、選択状態、キーボード操作を備える。
- ReferencePaneとTauri: 戻る・進む・更新を実装し、内蔵ブラウザの実際のURLを反映する。URL入力・リンク遷移・リダイレクト・レイアウト変更で履歴や表示を不必要に破棄しない。IPCは信頼するメイン画面限定、HTTP(S)と既存のローカルURL拒否を維持する。
- ヘルプ: 実バージョンと導入手順を同梱してオフラインで利用できる。Wikiを公開しアプリから参照できる。配布版に別途サーバー起動が不要であることを説明する。
- 検証: 既存テスト・型チェック・lint、操作の回帰テスト、Web UIとLinuxネイティブの実操作、配布ビルドを確認して2.1.0として公開する。
- 対象外: 拡張マーケットプレイス、Extension API変更、保存形式移行、Web版のクロスオリジン制限回避、追加バックエンド。

## v2.2.0 実装・受け入れ条件

- 設定: 「表示」「エディタ」「Web参照」「OpenAI」に整理し、保存で反映、キャンセルで破棄する。既存設定を保持し、新規項目は既定値で補完する。同一画面と他タブの変更に追従する。
- 表示: お手本・解説を設定と各ペインの閉じるボタンで非表示にでき、写経エディタは常に表示する。全6レイアウトで空き領域を詰め、元の配置・比率、編集内容・Undo・閲覧位置を保持する。Tauriの子Webviewも連動して隠し、復帰時に位置とサイズを更新する。
- テーマと文字: テーマアイコンのメニューでライト・ダーク・OS連動を即時適用する。コード文字サイズは10〜32px、初期値14px、1px刻みで縮小・拡大・リセットし、写経エディタ・テキストのお手本・貼り付け入力に共通適用する。
- 編集: スペース／タブ、幅2・4・8、自動インデント（既定: スペース2・有効）を以後の入力に適用する。既存コードは変換しない。ノーマル・Vim・Emacs・VSCodeモードを追加し、内容・選択・Undoを維持する。主要キーをショートカット一覧で示し、Escapeと日本語IME入力の競合を避ける。
- Web参照: URL欄横にフォルダ形ブックマークと反時計回り矢印の履歴アイコンを配置する。URL欄フォーカスで新しい順の履歴候補を表示し、URLで絞り込む。矢印・Enter・Escapeに対応し、一覧との重複表示を防ぐ。設定で候補を無効にしても最大20件の記録・重複除去・個別／全削除を維持する。TauriでメニューがWebviewの裏に隠れないようにする。
- ヘルプと教材: 項目を折りたたみ式にし、使い方のみ初期表示で開く。更新履歴はタグ・コミットに基づきv1.0.0〜v2.2.0を新しい順で同梱し、バージョン単位で開閉する。SQL6題（SELECT、WHERE／ORDER BY、GROUP BY／HAVING、JOIN、サブクエリ、CTE）を日本語の目的・テーブル／列説明付きで追加する。SQL実行は追加しない。
- 検証: 設定保存・キャンセル・旧データ補完、全6レイアウトの表示切替と状態保持、編集モード・IME・インデント、Web候補・一覧、テーマ・文字サイズ・ヘルプ・SQL選択を確認する。テスト・型チェック・lint、ブラウザとLinux Tauriの操作、800×600での主要操作を確認する。状態消失リスクは独立レビューを行う。未実施の実機確認は未検証として報告する。
- レビュー反映: お手本の「参照…」とファイルのドラッグ＆ドロップ、言語選択横のインデント幅、ページを押し下げない履歴候補と左側の履歴アイコン、保存成功のスナックバーを追加する。
- ショートカット: Ctrl / Cmd + / でモーダルを開き、機能別の入力欄で単一のキー組合せを割り当て、保存・キャンセル・初期値復帰を提供する。VSCodeを参考にした初期値を使い、メニューと競合するコメントはCtrl / Cmd + Shift + /。予約・重複・文字入力を妨げる割り当てを拒否する。Vim・Emacsには変更済みの割り当てのみ優先適用する。
- OpenAI: モデルをドロップダウンで選択する。既存の保存済みモデルは維持する。送信内容・保存・学習利用方針、モデル変更に伴う料金差と上限を強制しないことを明記する。
- 対象外: 画像付きの詳細ヘルプ（後日）、Extension API変更、入力済みコードの自動整形、コマンドパレット、SQL実行、追加バックエンド、リリース公開・タグpush。

## v2.3.2 実装・受け入れ条件

- お手本閲覧: PDFとEPUBのページ送りはボタン全体で操作でき、目次のスクロールは本文へ伝播しない。PDFの文書内リンクとEPUBの章・脚注リンクで移動できる。EPUB内の画像・コード・スタイルを表示し、外部通信と埋め込みスクリプトは許可しない。ファイルを続けて開く、読み込み中に閉じる、現在の参照ファイルだけを閉じる操作で古い読み込み結果が復活しない。
- 編集と設定: Ctrl/Cmd+Nで新しい空ページを作り、未保存内容があれば確認する。置換パネルは時間経過だけで閉じない。ファイル保存の操作後に緑のチェックと「Saved!」を示す。設定の説明ツールチップを画面内に収める。動作を保証できない「常に最前面」設定は削除する。
- APIキー: ブラウザ版では実行中のメモリ、デスクトップ版ではOSの資格情報ストアを使う。旧localStorageのキーは移行し、秘密情報をログ・URL・コミットへ含めない。デスクトップ版のOpenAI通信は信頼するメイン画面からのTauriコマンドに限定する。
- CIと配布: Web・Rust・ブラウザ操作・依存関係の検証をPRで実行し、mainの保護とリリースタグ保護を設定する。`v2.3.2`タグからWeb成果物とWindows/macOS/Linuxデスクトップ成果物を作り、デスクトップ配布の署名・macOS公証、チェックサムと来歴を確認してから公開する。署名資格情報がない間はタグ作成と公開を保留する。
- 検証: 単体テスト、型チェック、lint、ブラウザ操作、Rustの検査、独立レビューを通す。Windows/macOS実機操作とOS資格情報ストアの実環境確認は実施結果を明記する。
- 対象外: 正誤判定・統計・学習進捗保存、追加バックエンド、署名のないデスクトップ公開。
