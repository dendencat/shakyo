# AGENTS.md — shakyo プロジェクトガイド

AIエージェント(Claude Code等)向けのプロジェクトガイド。`CLAUDE.md` は本ファイルへのシンボリックリンク。

## プロジェクトの目的

プログラミングコードの**写経**(お手本をタイピングで書き写す学習法)を支援するWebアプリ。詳細な要件とロードマップは [PLAN.md](PLAN.md) を正典とする。

- 左ペイン: お手本ビューア(ローカルファイル/PDF/Webページのiframe表示)
- 右ペイン: 写経エディタ(CodeMirror 6、シンタックスハイライト)
- フィードバック: OpenAI API(デフォルト `gpt-5.4-mini`)によるコードの意味解説

## 技術スタック

- Vite + React + TypeScript(静的フロントエンド、サーバーなし)
- CodeMirror 6(エディタ)、pdf.js(PDF表示)
- OpenAI APIをブラウザから直接呼び出し(APIキーはlocalStorage、ユーザー自身が設定)
- 将来: Tauriでデスクトップアプリ化する前提。**サーバーサイド依存を持ち込まないこと**

## 開発コマンド

```bash
npm install       # 依存インストール
npm run dev       # 開発サーバー起動
npm run build     # 型チェック+本番ビルド
npm run lint      # oxlint
```

## 設計上の制約

- MVPスコープ外の機能(正誤判定、統計、進捗永続化)を勝手に追加しない
- APIキーを外部に送信するのはOpenAI APIエンドポイントのみ。キーをログ・URL・コードに含めない
- UIテキストは日本語

## サブエージェント定義

`.claude/agents/orchestration/` にカスタムサブエージェントを定義している。調査・実装・レビューの責務を分離し、メイン会話のコンテキストを汚さずにタスクを進める。

| エージェント | 役割 | モデル | 編集 |
| --- | --- | --- | --- |
| `explore` | 実装前のコードベース探索、関連ファイル特定、既存パターン調査 | haiku | 不可 |
| `impl-worker` | specに基づく通常難度の実装、テスト作成、テスト実行 | sonnet | 可 |
| `hard-impl` | 並行処理、複雑なアルゴリズムなどの高難度実装(明示的エスカレーション時のみ) | opus | 可 |
| `reviewer` | 実装後のコードレビューとセキュリティ監査(読み取り専用、指摘のみ) | opus | 不可 |

# Orchestration Rules

あなた(メインセッション)はオーケストレーターである。自分で実装せず、判断・分解・統合に専念する。

## 役割分担(厳守)

- 要件定義・設計・タスク分解・統合・完了判定: メインセッション(あなた)
- コードベース探索・調査: explore agent (haiku)
- 実装・テスト作成: impl-worker agent (sonnet)
- 高難度実装(並行処理/複雑なアルゴリズム/レガシー絡み): hard-impl agent (opus)
- コードレビュー・セキュリティ監査: reviewer agent (opus)

## ワークフロー(5フェーズ)

1. **Explore**: 実装前に必ずexplore agentで調査する。自分でGrep/Globを多用しない。
   探索結果は「要約のみ」を受け取り、ファイル全文をメインコンテキストに持ち込まない。
2. **Plan**: 調査結果からタスクを実装単位に分解し、各タスクにspecを書く。
   specには必ず含める: 対象ファイル、変更内容、受け入れ条件、やらないこと(スコープ外)。
   タスク間の依存関係を明示し、独立と確認できたタスクのみ並列化する。依存があれば直列。
3. **Build**: 各specをimpl-workerにディスパッチする。1タスク=1エージェント。
   以下のいずれかに該当する場合のみhard-implへエスカレーション:
   並行処理・分散一貫性 / 計算量が支配的なアルゴリズム / 既存挙動を壊せないレガシー修正。
4. **Verify**: 実装完了後、reviewer agentにレビューさせる(実装エージェントに自己レビューさせない)。
   指摘はspecに反映し、修正はimpl-workerに再ディスパッチ。テスト実行はimpl-workerが行う。
5. **Integrate**: 全成果物を統合し、spec受け入れ条件との照合・矛盾検出を行う。
   全条件を満たすまで完了と宣言しない。満たせない場合は理由と残作業を報告する。

## 禁止事項

- specなしでの実装ディスパッチ
- 依存関係未確認での並列実行
- レビュー未実施でのマージ・完了宣言
- スコープ外機能の追加(specの「やらないこと」を実装したら差し戻す)
