# v2.1.0 検証記録

2026-09-12、Linux/WSL Ubuntu 26.04、Node 22.22.0、Rust 1.98.1で検証。

| 要件 | 確認した証拠 |
| --- | --- |
| 別途バックエンド不要 | TauriのfrontendDistはdist。生成バイナリがtauri://localhostから同梱UIを表示し、WebDriverで操作できた |
| 開閉式サイドバー | App回帰テストとChromiumで開閉・切替・本文保持・各機能の操作を確認 |
| 指定アイコン | 共通SVG。常時テキストなし、title・aria-labelあり。ライト/ダーク画面を目視確認 |
| ファイル・保存 | 実ブラウザでサンプルを開き、写経し、名前を付けて保存 |
| ブラウザ操作とURL | 実Tauriでページ起点遷移、戻る、進む、更新、HTTPリダイレクト、同じURLの再入力を確認。実際のHTTP受信とURL欄で照合 |
| 表示の維持 | URL変更で子WebViewを破棄しないテスト。実Tauriでサイドバー開閉後も遷移先URLを維持 |
| 拡張機能 | 実ブラウザでtext-toolsを検査・承認・導入・実行。API/保存形式は変更なし |
| ヘルプ・バージョン | package.jsonの2.1.0を表示。Chromiumをオフラインにして同梱手順を展開。ネイティブにもヘルプを同梱 |
| 画面幅 | 1280x800と800x600。全体の横方向へのはみ出しなし |

- npm test -- --silent: 315 passed（31 files）
- npm run lint: 0 warnings / 0 errors
- npm run build: 型チェック・本番ビルド成功（既存の大きなJSチャンクに関する注意は継続）
- cargo test --manifest-path src-tauri/Cargo.toml --locked: 5 passed
- npm run tauri build -- --debug --no-bundle: 成功

差分の見直しでは、メインWebViewのラベルとorigin検証、HTTP(S)・既存ローカルURL拒否、固定スクリプトによる履歴操作、古いURL応答の無視、既存の拡張権限と保存形式の維持を確認。オーケストレーションは使わずメインセッションで実装・見直しを実施した。

Web版のクロスオリジンiframeはアプリから開いたURLだけを履歴操作対象にする。Windows/macOSの実機操作は未検証。配布ビルドとWiki公開状態はGitHub上の最終結果で別途確認する。

追加検証: 配布Workerの分割チャンクが入口を再importし、WebKitで受信ハンドラーが上書きされる問題を検出。WorkerのcodeSplittingを無効にし、Linux実アプリで導入済みtext-toolsの起動・確認UI・大文字変換が成功した。ビルド後にASTからWorkerの外部JS importがないことを検証するチェックを追加し、再発を防ぐ。
